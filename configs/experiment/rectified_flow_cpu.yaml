# @package _global_

defaults:
  - override /model: diffusion
  - override /data: dummy

model:
  diffusion_objective: "rectified_flow"

trainer:
  max_steps: 10000
  max_epochs: -1
  accelerator: "cpu"
