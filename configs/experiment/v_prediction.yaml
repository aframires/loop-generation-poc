# @package _global_

defaults:
  - override /model: diffusion
  - override /data: dummy

model:
  diffusion_objective: "v_prediction"

trainer:
  max_steps: 10000
  max_epochs: -1
  val_check_interval: 500